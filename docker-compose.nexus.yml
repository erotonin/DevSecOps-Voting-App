version: '3.8'
services:
  nexus:
    image: sonatype/nexus3
    container_name: nexus
    ports:
      - "8081:8081"
    volumes:
      - nexus_data:/nexus-data
    networks:
      - nexus_net
networks:
  nexus_net:
    driver: bridge
volumes:
  nexus_data: